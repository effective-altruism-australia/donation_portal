{
  "name": "eaa-donation-form",
  "version": "1.0.0",
  "description": "A simple dependency free donation form",
  "engines": {
    "node": ">=22.9.01"
  },
  "main": "build.mjs",
  "scripts": {
    "build": "node build.js",
    "dev": "node --env-file=.env --watch-path=./src build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nathan Sherburn",
  "license": "ISC",
  "dependencies": {}
}
