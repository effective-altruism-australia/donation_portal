import { expect, test } from "@playwright/test";

/*
Ensure that selecting "This is a gift" reveals the gift recipient fields and
that the form is submitted with the correct data.
*/

test("Gifting: sending a gift works", async ({ page }) => {
  // Placeholder test for when we add gift functionality back
  expect(true).toBe(true);
});
